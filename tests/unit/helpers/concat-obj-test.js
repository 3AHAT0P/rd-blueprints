import { concatObj } from 'dummy/helpers/concat-obj';
import { module, test } from 'qunit';

module('Unit | Helper | concat obj');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = concatObj([42]);
  assert.ok(result);
});
